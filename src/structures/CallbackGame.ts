import Client from '../client/Client'
import Base from './Base'

/** A placeholder, currently holds no information */
export default class CallbackGame extends Base {
	constructor(client: Client) {
		super(client)
	}
}